# CleverNote Sway Configuration
# Add this to your ~/.config/sway/config

# Start daemon on login
exec clevernote-daemon

# Bind Ctrl+Space to toggle recording
bindsym Ctrl+Space exec clevernote toggle

